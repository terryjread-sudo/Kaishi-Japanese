{
  "version": "11.8.47",
  "released": "2026-08-25",
  "title": "Flight Mode & Offline Session Loop Fix",
  "changes": [
    "Fixed infinite session recursion loop when accessing learning in flight mode or offline.",
    "Replaced network-only {cache: 'no-store'} data fetches with resilient Cache API fallbacks so offline startups load data smoothly.",
    "Updated Service Worker networkFirst fallback with ignoreSearch:true so cached scripts and styles match when offline."
  ]
}
