{
  "version":"11.25.3",
  "released":"2026-08-30",
  "title":"Duplicate Dashboard button on the Journey screen",
  "changes":[
    "The floating Dashboard button was rendering directly on top of the Journey screen's own inline \u2190 Dashboard button, making it look like there were two. The original inline button is now hidden once the floating overlay installs (it still works as a fallback if that script ever fails to load)."
  ]
}
